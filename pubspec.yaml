name: cli
description: A sample command-line application.
version: 1.0.0
# repository: https://github.com/my_org/my_repo

environment:
  sdk: ^3.12.0

# Add regular dependencies here.
dependencies:
  path: ^1.9.0

dev_dependencies:
  lints: ^6.0.0
  melos: ^7.8.0
  test: ^1.25.6
  git_hooks: ^1.0.2 # Confira a versão mais recente no pub.dev

melos:
  useRootAsPackage: true
  command:
  version:
    # Ativa o suporte estrito a Commits Convencionais para versionamento e changelogs
    conventionalCommits: true
